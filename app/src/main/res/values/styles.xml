<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dialog theme. -->
    <style name="AlertDialogCustom" parent="@android:style/Theme.Dialog">
        <item name="android:textColor">#FFFF00</item>
        <item name="android:fontFamily">@font/answer</item>
        <item name="android:textSize">25sp</item>
        <item name="android:windowBackground">@drawable/rect_round_white</item>
    </style>

    <!-- Custom theme. -->
    <style name="CustomTheme" parent="@android:style/Theme.Holo">
        <item name="android:listViewStyle">@style/CustomListView</item>
        <item name="android:textColor" >#FFFF00</item>
    </style>

    <style name="CustomListView" parent="@android:style/Widget.ListView">
        <item name="android:background">@color/colorMainText</item>
        <item name="android:fastScrollEnabled">true</item>
    </style>

    <style name="ListFont" parent="@android:style/Widget.ListView">
        <item name="android:textColor">#FF0000</item>
        <item name="android:typeface">sans</item>
    </style>

</resources>
